{
  "name": "sentryguard-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker backend for SentryGuard: Tesla Fleet Telemetry ingestion, partner-domain registration, and public key hosting.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.6.3",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
